# Production Environment Configuration
# This file shows the environment variables needed for production deployment
# DO NOT commit actual values - this is just a template!

# ============================================
# NETLIFY (Frontend) Environment Variables
# ============================================
# Set these in: Netlify Dashboard → Site Settings → Environment Variables

# Backend API URL (your Render backend URL)
VITE_API_BASE_URL=https://your-backend-app.onrender.com

# ============================================
# RENDER (Backend) Environment Variables
# ============================================
# Most are configured in render.yaml, but sensitive ones need manual setup

# Server Configuration (Auto-configured by Render)
NODE_ENV=production
PORT=10000
HOST=0.0.0.0

# Database (Auto-configured from Render PostgreSQL)
POSTGRES_URI=postgresql://user:password@host:5432/database
# OR individual parameters:
# POSTGRES_HOST=
# POSTGRES_PORT=5432
# POSTGRES_DB=
# POSTGRES_USER=
# POSTGRES_PASSWORD=

# JWT Secret (Auto-generated by Render, or set manually)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_production_jwt_secret_minimum_32_characters

# Twilio Configuration (Set manually in Render Dashboard)
# Get from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_VERIFY_SERVICE_SID=your_verify_service_sid

# Push Notifications VAPID Keys (Set manually in Render Dashboard)
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key

# ============================================
# Security Checklist for Production
# ============================================
# [ ] All secrets are 32+ characters and randomly generated
# [ ] Database credentials use strong passwords
# [ ] CORS is properly configured for your frontend domain
# [ ] Rate limiting is enabled on sensitive endpoints
# [ ] HTTPS is enforced (Netlify and Render do this by default)
# [ ] Environment variables are set in platform dashboards, not in code
# [ ] .env files are in .gitignore
# [ ] Database backups are configured
# [ ] Error logging/monitoring is set up (optional: Sentry, LogRocket, etc.)
